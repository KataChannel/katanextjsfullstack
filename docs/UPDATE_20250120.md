# Tóm tắt các thay đổi - 20/11/2025

## 1. Upload Media - Tăng giới hạn lên 20MB

**Files:** 
- `app/api/media/route.ts`
- `app/admin/media/media-upload-form.tsx`
- `app/admin/media/page.tsx`
- `next.config.ts`

**Thay đổi:**
- Tăng `maxSize` từ 10MB → 20MB
- Thêm `middlewareClientMaxBodySize: '25mb'` trong next.config
- Sử dụng XMLHttpRequest thay vì fetch() cho upload file lớn
- Thêm logging chi tiết để debug

## 2. Cập nhật Footer InnerBright

**File:** `components/footer.tsx`

**Thay đổi:**
- Logo cố định: `http://116.118.49.243:12007/innerbright/1763620956393-ajrdh.webp`
- 4 cột: Logo & Contact | INNNER | OUR SERVICES | Social Icons
- Phone: 090 837 09 68
- Email: info@innerbright.vn
- Background trắng, typography đúng chuẩn

## 3. Cập nhật Header Logo

**File:** `components/header.tsx`

**Thay đổi:**
- Logo cố định: `http://116.118.49.243:12007/innerbright/1763620949996-bvu1vi.webp`
- Loại bỏ conditional rendering

## 4. Trang Bộ thẻ NLP (/bo-the-nlp)

**Files:**
- `app/(public)/bo-the-nlp/components/HeroSection.tsx`
- `app/(public)/bo-the-nlp/components/WhyChooseNLPSection.tsx`

**Thay đổi:**
- HeroSection: 3 tấm card NLP xếp chồng với positioning chính xác
  - Card 1 (Green): top-[100px] left-0 -rotate-12
  - Card 2 (Orange): top-10 left-20 rotate-[7deg] (lớn nhất)
  - Card 3 (White/Blue): top-0 left-[180px] rotate-[15deg]
- WhyChooseNLPSection: Thay 3 cards bằng 1 hình đơn giản

## 5. Fix Homepage Redirect Feature

**Files:**
- `app/(public)/page.tsx`
- `app/api/website-settings/route.ts`

**Thay đổi:**
- Thêm validation `homeRedirect.trim() !== ''`
- Thêm logging chi tiết ở cả client và API
- Fix: Đảm bảo redirect chỉ chạy khi có giá trị hợp lệ

**Cách sử dụng:**
1. Vào `/admin/settings` → Tab "SEO"
2. Nhập URL vào "Homepage Redirect" (VD: `/ve-innerbright`)
3. Lưu cài đặt
4. Truy cập `/` → Tự động redirect

## 6. Fix Delete Button trong /admin/pages-v2

**Files:**
- `app/admin/pages-v2/page.tsx`
- `app/admin/pages-v2/delete-button.tsx` (NEW)

**Thay đổi:**
- Tạo client component `DeletePageButton` với AlertDialog
- Confirm dialog trước khi xóa
- Loading state khi đang xóa
- Toast notification sau khi xóa thành công
- Auto refresh danh sách sau khi xóa

**Tính năng:**
- ✅ Confirm dialog với tên trang
- ✅ Loading spinner khi xóa
- ✅ Error handling với toast
- ✅ Router refresh để cập nhật UI

## Code Quality

**Principles áp dụng:**
- ✅ Clean Architecture
- ✅ Mobile First + Responsive
- ✅ TypeScript strict
- ✅ Shadcn UI components
- ✅ Error handling đầy đủ
- ✅ User-friendly notifications
- ✅ Logging để debug

## Testing

**Cần test:**
1. Upload file 15-20MB → Check server logs
2. Homepage redirect → Check redirect hoạt động
3. Delete page trong pages-v2 → Confirm dialog → Xóa thành công
4. Footer & Header hiển thị logo đúng
5. Trang /bo-the-nlp hiển thị 3 cards đúng vị trí
